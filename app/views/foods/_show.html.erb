<tr>
  <td><%= food.name %></td>
  <td><%= food.measurement_unit %></td>
  <td><%= food.price %></td>
  <td class="del_btn">
          <div>
            <%= button_to 'Remove', user_food_path(current_user, food.id), method: :delete, class: "btn btn-danger" %>
          </div>
      <span>No actions</span>
  </td>
</tr>